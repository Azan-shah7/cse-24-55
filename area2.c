#include <stdio.h>
int main() {
    int r = 5, h = 10;
    float pi = 3.14;
    printf("The area of circle is %.2f\n", pi * r * r);
    printf("The volume of cylinder is %.2f", pi * r * r * h);
    return 0;
}
